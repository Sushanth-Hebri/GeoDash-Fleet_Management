import { Container, Typography } from "@mui/material";

const Dashboard = () => {
  return (
    <Container>
      <Typography variant="h4" mt={5}>Fleet Tracking Dashboard</Typography>
      <Typography variant="body1">Coming Soon...</Typography>
    </Container>
  );
};

export default Dashboard;
